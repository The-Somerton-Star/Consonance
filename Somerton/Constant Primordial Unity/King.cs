namespace Somerton.Constant_Primordial_Unity
{
    public class King
    {
        public King(char word)
        {
            // 9 factoring 288 by five, in splitting chance for wisdom trine, to accommodate bit relevance consigning Proximal Words; corresponding to Magnitude
            Flux = AttainedRealQuanta.Innate(word); // X4 @Theory = K.C
            Instantiation = AttainedRealQuanta.Innate(word); // E = B.10
            Unification = AttainedRealQuanta.Innate(word); // = D.A
            Universality = AttainedRealQuanta.Innate(word); // = Q.N
            Expression = AttainedRealQuanta.Innate(word); // = G.E
            Light = AttainedRealQuanta.Innate(word); // = C.1
            Magnitude = AttainedRealQuanta.Innate(word); // M; Gaze @Omega = M.12
        }
        private Heartart.Interfaces.IHeartart AttainedRealQuanta { get; }
        private char Flux { get; }
        private char Instantiation { get; }
        private char Unification { get; }
        private char Universality { get; }
        private char Expression { get; }
        private char Light { get; }
        public char Magnitude { get; }
    }
}
