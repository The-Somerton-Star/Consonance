apiVersion: v1
kind: Service
metadata:
  name: {{ template "ecocloud.fullname" . }}
  labels:
    app: {{ template "ecocloud.name" . }}
    chart: {{ template "ecocloud.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "ecocloud.name" . }}
    release: {{ .Release.Name }}
